using UnityEngine;

namespace Riftbourne.Combat
{
    public class TurnManager : MonoBehaviour
    {
        private void Start()
        {
            Debug.Log("Turn Manager: Ready for combat");
        }

        // We'll add turn logic later
    }
}